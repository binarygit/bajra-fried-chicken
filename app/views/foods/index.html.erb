<main class="container">
  <ul class='reservations'>
  <% @foods.each do |food| %>
    <li>
      <p><%= food.name %></p>
      <p><%= food.description %></p>
      <p>Rs.<%= food.price %></p>
    </li>
  <% end %>
  </ul>
</main>
